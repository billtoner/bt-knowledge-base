# TODO

Backlog for the next week or so. Check items off as they land; move finished
ones to Done with the date.

## Backlog

- [ ] Revise category names to be `-`-separated single strings (no spaces) —
  e.g. `AWS CLI` → `AWS-CLI`, `File and Directory` → `File-and-Directory`.
  Touches the H1 in each `doc/categories/<slug>.md` and the link text in the
  top-level index (`doc/bt-knowledge-base.md`); `kb cats` / `kb list` read the
  H1, so they'll reflect it automatically. Open question to settle first:
  keep the original casing (`AWS-CLI`) or fully lowercase to match the file
  slugs (`aws-cli`)?

## Done
